{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "/usr/bin/clang++", // Or g++ if you prefer GCC
            "args": [
                "-g", // For debugging symbols
                "-std=c++17", // Change the C++ version if necessary
                "${workspaceFolder}/main.cpp", // Path to your C++ source file
                "-o", "${workspaceFolder}/your_program_name" // Output executable name
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "detail": "Generated task by VS Code"
        }
    ]
}